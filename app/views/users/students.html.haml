%table{style:'margin: auto; width:85%'}

  %thead
    %th{colspan:6}
      %h1.center-text Students

  %tr
    %th Name
    %th Dob
    %th Address
    %th Balance
    %th

  - @students.each do |student|
    %tr
      %td= link_to student.name, student
      %td= student.dob
      %td= student.address
      %td= student.balance
      %td
        = link_to 'Edit', edit_user_path(student), class:'tiny button'
        = link_to 'Destroy', student, :method => :delete, :data => { :confirm => 'Are you sure?' }, class:'tiny button'

  %tr
    %td.center-text{colspan:6}
      = link_to 'New Student', new_user_path, class:'button'
