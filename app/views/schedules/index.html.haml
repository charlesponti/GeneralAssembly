%table{style:'margin: auto; width:85%'}
  %thead
    %th{colspan: 4}
      %h1.center-text Schedules

  %tr.center-text
    %td Room
    %td Dates
    %td Course
    %td Times

  - @schedules.each do |s|
    %tr.center-text
      %td= s.room.name
      %td= "#{s.start_date.strftime("%m-%d-%Y")} - #{s.end_date.strftime("%m-%d-%Y")}"
      %td= s.course.name
      %td= s.print_times